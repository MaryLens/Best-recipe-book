package com.project.recipebook.repositories;

import com.project.recipebook.models.ImageToStep;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageToStepRepository extends JpaRepository<ImageToStep,Long> {
}
